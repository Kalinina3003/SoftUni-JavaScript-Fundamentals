// Task: Print Characters.
// Description: Receive a string.
// Print each character from the string.
// Print every character on a separate line.

function printCharacters(str) {
    for (let letter of str) {
        console.log(letter);
    }
}
printCharacters('AWord');
